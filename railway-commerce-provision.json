{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile"
  },
  "deploy": {
    "startCommand": "pnpm tsx ./scripts/provision-commerce-roles.ts",
    "restartPolicyType": "NEVER"
  }
}
