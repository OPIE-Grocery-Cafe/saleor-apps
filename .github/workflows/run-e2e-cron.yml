name: OPIE fork E2E schedule guard

on:
  schedule:
    - cron: "7 7 * * 1-5"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  explain:
    runs-on: ubuntu-22.04
    steps:
      - name: Confirm the fork-specific test boundary
        run: |
          echo "The upstream Saleor Cloud E2E matrix is intentionally disabled in the OPIE fork."
          echo "Use the required PostgreSQL integration workflow and the protected Stripe staging matrix."
