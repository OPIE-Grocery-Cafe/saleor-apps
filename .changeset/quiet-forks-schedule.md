---
---

The OPIE fork no longer runs Saleor's upstream cloud E2E schedule without the upstream-only secrets and services; its protected PostgreSQL and Stripe staging workflows remain the release gates.
